diff --git a/list.h b/list.h
index 9bc05a7..57670af 100644
--- a/list.h
+++ b/list.h
@@ -27,6 +27,41 @@
 #include <stddef.h>
 #endif

+/* NuttX exposes its intrusive-list helpers as macros through stdio.h.
+ * QuickJS has private helpers with the same names, so remove only those
+ * macros before declaring the QuickJS implementation. */
+
+#ifdef LIST_HEAD_INIT
+#  undef LIST_HEAD_INIT
+#endif
+#ifdef list_entry
+#  undef list_entry
+#endif
+#ifdef list_add
+#  undef list_add
+#endif
+#ifdef list_add_tail
+#  undef list_add_tail
+#endif
+#ifdef list_del
+#  undef list_del
+#endif
+#ifdef list_empty
+#  undef list_empty
+#endif
+#ifdef list_for_each
+#  undef list_for_each
+#endif
+#ifdef list_for_each_safe
+#  undef list_for_each_safe
+#endif
+#ifdef list_for_each_prev
+#  undef list_for_each_prev
+#endif
+#ifdef list_for_each_prev_safe
+#  undef list_for_each_prev_safe
+#endif
+
 struct list_head {
     struct list_head *prev;
     struct list_head *next;
