############################################################################
# apps/system/desktop/Make.defs
############################################################################

ifneq ($(CONFIG_SYSTEM_DESKTOP),)
CONFIGURED_APPS += $(APPDIR)/system/desktop
endif
